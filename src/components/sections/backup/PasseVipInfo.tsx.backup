export default function PasseVipInfo() {
  const vantagens = [
    "Preço fixo mesmo se a promoção acabar",
    "Garantia de vaga",
    "Serviço bônus na visita"
  ]

  const comoFunciona = [
    {
      numero: "1",
      texto: "Escolha o serviço"
    },
    {
      numero: "2", 
      texto: "Pague a entrada (Pix ou cartão)"
    },
    {
      numero: "3",
      texto: "Receba seu código VIP no WhatsApp"
    },
    {
      numero: "4",
      texto: "No dia, pague o restante"
    }
  ]

  return (
    <section id="passe-vip" className="vogue-section" style={{backgroundColor: '#3B3B3A', color: '#FEFEFE'}}>
      <div className="vogue-container">
        <div className="grid lg:grid-cols-2 gap-20 items-start">
          
          {/* Por que vale a pena */}
          <div>
            <div className="text-center lg:text-left mb-12">
              <div className="vogue-caption mb-4" style={{color: '#444e55'}}>VANTAGENS EXCLUSIVAS</div>
              <h2 className="vogue-subhead mb-8" style={{color: '#FEFEFE'}}>
                POR QUE VALE A PENA O PASSE VIP
              </h2>
              <div className="vogue-divider-long mb-8 lg:mx-0 mx-auto" style={{backgroundColor: '#FEFEFE', opacity: 0.3}}></div>
            </div>
            
            <div className="space-y-6">
              {vantagens.map((vantagem, index) => (
                <div key={index} className="flex items-start gap-4">
                  <div 
                    className="flex-shrink-0 w-8 h-8 rounded-full flex items-center justify-center text-sm font-bold"
                    style={{backgroundColor: '#444e55', color: '#FEFEFE'}}
                  >
                    ✓
                  </div>
                  <div className="vogue-body text-lg" style={{color: '#FEFEFE'}}>
                    {vantagem}
                  </div>
                </div>
              ))}
            </div>
          </div>
          
          {/* Como funciona */}
          <div>
            <div className="text-center lg:text-left mb-12">
              <div className="vogue-caption mb-4" style={{color: '#444e55'}}>PROCESSO SIMPLES</div>
              <h2 className="vogue-subhead mb-8" style={{color: '#FEFEFE'}}>
                COMO FUNCIONA
              </h2>
              <div className="vogue-divider-long mb-8 lg:mx-0 mx-auto" style={{backgroundColor: '#FEFEFE', opacity: 0.3}}></div>
            </div>
            
            <div className="space-y-8">
              {comoFunciona.map((passo, index) => (
                <div key={index} className="flex items-start gap-6">
                  <div 
                    className="flex-shrink-0 w-12 h-12 rounded-full flex items-center justify-center text-lg font-bold"
                    style={{backgroundColor: '#444e55', color: '#FEFEFE'}}
                  >
                    {passo.numero}
                  </div>
                  <div className="pt-2">
                    <div className="vogue-body text-lg" style={{color: '#FEFEFE'}}>
                      {passo.texto}
                    </div>
                  </div>
                </div>
              ))}
            </div>
            
            <div className="mt-12 text-center lg:text-left">
              <button 
                onClick={() => document.getElementById('agendamento')?.scrollIntoView({ behavior: 'smooth' })}
                className="px-12 py-4 transition-all duration-300 text-lg"
                style={{
                  backgroundColor: '#444e55',
                  color: '#FEFEFE',
                  border: 'none',
                  fontFamily: 'Garet, sans-serif',
                  fontSize: '14px',
                  fontWeight: 600,
                  letterSpacing: '0.1em',
                  textTransform: 'uppercase',
                  cursor: 'pointer',
                  boxShadow: '0 4px 20px rgba(68, 78, 85, 0.4)'
                }}
                onMouseEnter={(e) => {
                  e.currentTarget.style.backgroundColor = '#FEFEFE'
                  e.currentTarget.style.color = '#3B3B3A'
                  e.currentTarget.style.transform = 'translateY(-2px)'
                  e.currentTarget.style.boxShadow = '0 8px 30px rgba(68, 78, 85, 0.6)'
                }}
                onMouseLeave={(e) => {
                  e.currentTarget.style.backgroundColor = '#444e55'
                  e.currentTarget.style.color = '#FEFEFE'
                  e.currentTarget.style.transform = 'translateY(0px)'
                  e.currentTarget.style.boxShadow = '0 4px 20px rgba(68, 78, 85, 0.4)'
                }}
              >
                GARANTIR MEU PASSE VIP AGORA
              </button>
            </div>
          </div>

        </div>
      </div>
    </section>
  )
}
